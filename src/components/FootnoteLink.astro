---
interface Props {
  number: number;
}

const { number } = Astro.props;
---

<a
  class="mx-0.5 text-orange-500 dark:text-orange-400"
  href={`#${number.toString()}`}><sup>{number.toString()}</sup></a>
